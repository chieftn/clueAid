import * as React from 'react';
import { PrimaryButton } from '@fluentui/react';

export const GameCreate: React.FC = () => {
    return (
        <>
            <div>Create Game</div>
            <PrimaryButton
                text="hello world"
            />
        </>
    );
};