import type { IGroup } from '@fluentui/react';
import { Assertion } from './model';
import { deck } from '../shared/constants';

export const transcribeAssertions = (assertions: Assertion[]): IGroup[] => {
    return [];
}